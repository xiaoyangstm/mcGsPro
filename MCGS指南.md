# MCGS组态软件完整学习指南

> 本文档基于实际项目（三桥品牌15寸触屏FFU群控系统）整理，涵盖从入门到实战的完整流程。

---

## 目录

- [一、MCGS基础认知](#一mcgs基础认知)
- [二、环境搭建与程序下载](#二环境搭建与程序下载)
- [三、网络配置与通信](#三网络配置与通信)
- [四、三协议FFU通信](#四三协议ffu通信)
- [五、MCGS四大配置详解](#五mcgs四大配置详解)
- [六、按钮控制到底层通信](#六按钮控制到底层通信)
- [七、系统初始化与交互流程](#七系统初始化与交互流程)
- [八、常用脚本函数速查](#八常用脚本函数速查)
- [九、常见问题排查](#九常见问题排查)

---

## 一、MCGS基础认知

### 1.1 什么是MCGS

MCGS（昆仑通态McgsPro）是一款国产嵌入式组态软件，用于开发触摸屏HMI人机界面。

### 1.2 项目文件结构

| 文件类型 | 说明 |
|---------|------|
| `.MCP` | MCGS项目文件（基于Access/Jet数据库） |
| `.mdr` | MCGS设备驱动文件 |
| `.MCP.bak` | 自动备份文件 |

### 1.3 五大工作区

| 工作区 | 作用 | 类比 |
|--------|------|------|
| **用户窗口** | 画交互界面 | 决定"怎么说" |
| **设备窗口** | 配置通信驱动 | 决定"和谁说话" |
| **实时数据库** | 定义所有变量 | 决定"说什么内容" |
| **运行策略** | 脚本逻辑控制 | 决定"什么时候说" |
| **报警配置** | 报警条件与响应 | 安全监控 |

---

## 二、环境搭建与程序下载

### 2.1 安装McgsPro

1. 从昆仑通态官网下载McgsPro安装包
2. 安装完成后打开`.MCP`项目文件

### 2.2 程序下载到触摸屏

#### 方式一：TCP/IP网络下载（推荐）

1. 确保电脑和触摸屏在同一局域网
2. McgsPro → 文件 → 下载 → 选择TCP/IP方式
3. 输入触摸屏IP地址，点击下载

#### 方式二：U盘升级包

1. 插入U盘
2. McgsPro → 工具 → 制作U盘升级包
3. **必须选择U盘根目录**（如`E:\`），不能选子文件夹
4. 将生成的文件拷贝到U盘根目录
5. U盘插入触摸屏USB口，自动升级

### 2.3 常见问题：U盘路径不合法

**问题现象**：
```
路径'G:\package_sl'不是合法的U盘根目录,是否继续?
```

**原因**：
1. 选择了子文件夹而不是根目录
2. 目标盘符不是可移动磁盘（U盘）

**解决方法**：
- 必须选择U盘的**根目录**（如`E:\`）
- 必须插入真实的U盘，不能是硬盘分区

---

## 三、网络配置与通信

### 3.1 网线连接排查

**问题现象**：ping不通触摸屏

**排查步骤**：

1. **检查以太网适配器状态**
   ```cmd
   ipconfig
   ```
   确认"以太网适配器"显示"已连接"，不是"媒体已断开连接"

2. **常见原因**：
   - 网线松动（最常见）→ 重新插紧
   - 网线插错口 → 确认插在以太网口，不是串口
   - 以太网适配器被禁用 → `ncpa.cpl` → 右键启用

### 3.2 IP地址设置

#### 为什么要设置同一网段？

网段 = 同一个小区，主机号 = 门牌号

```
192.168.1.xxx  →  网段（小区名）
最后面的 xxx   →  主机号（门牌号）
```

| 设备 | IP地址 | 说明 |
|------|--------|------|
| 电脑 | 192.168.1.**200** | 同网段，不同主机号 |
| 触摸屏 | 192.168.1.**101** | 同网段，不同主机号 |

#### 设置步骤

1. `Win + R` → 输入`ncpa.cpl` → 回车
2. 右键"以太网" → 属性
3. 双击"Internet 协议版本 4 (TCP/IPv4)"
4. 设置：
   - IP地址：192.168.1.100（或200）
   - 子网掩码：255.255.255.0
   - 默认网关：192.168.1.1

#### ⚠️ 禁止设置相同IP

```
电脑：    192.168.1.101
触摸屏：  192.168.1.101
```
**IP冲突！两台设备无法通信！**

### 3.3 验证连通

```cmd
ping 192.168.1.101
```

- `回复来自 192.168.1.101...` → ✅ 通了
- `请求超时` → ❌ 检查网线和IP设置

### 3.4 防火墙问题

如果ping不通，尝试关闭防火墙：

```cmd
netsh advfirewall set allprofiles state off
```

测试完后再开启：

```cmd
netsh advfirewall set allprofiles state on
```

---

## 四、三协议FFU通信

### 4.1 三协议总览

| 协议 | 通信方式 | 规模 | 适用场景 |
|------|---------|------|---------|
| 协议1：RTU直连 | Modbus RTU/RS485 | 31台 | 小系统 |
| 协议2：RTU 5路中继 | Modbus RTU/RS485 | 155台(5×31) | 中系统 |
| 协议3：TCP触屏 | Modbus TCP/以太网 | 600台(10×60) | 大系统 |

### 4.2 协议3（TCP触屏）参数

本项目使用协议3：

| 参数 | 值 |
|------|-----|
| 通信方式 | Modbus TCP |
| IP地址 | 192.168.1.101 |
| TCP端口 | 502 |
| **Slave ID** | **0（固定）** |
| 最大FFU数 | 600台（10路×60台） |

### 4.3 Slave ID理解

**Slave ID ≠ 区域，Slave ID = 触屏控制器本身**

```
Slave ID = 0 (触屏控制器)
    │
    ├── 中继器1 → 第1~5路 (每路60台FFU)
    │
    └── 中继器2 → 第6~10路 (每路60台FFU)
```

**区分区域/设备靠寄存器地址，不是Slave ID。**

### 4.4 核心寄存器地址（协议3）

#### 单台FFU控制

| 寄存器范围 | 功能 | 读写 |
|-----------|------|------|
| 1601~1660 | 第1路单台开关 | R/W (1=开, 0=关) |
| 1231~1290 | 第1路转速设定 | R/W (400~1500 RPM) |

#### 单台FFU状态读取

| 寄存器范围 | 功能 | 读写 |
|-----------|------|------|
| 0001~0060 | 第1路设备状态 | R (0=停止, 1=运行, ≥2=故障) |
| 0301~0360 | 第1路实际转速 | R (RPM) |
| 0601~0660 | 第1路实际功率 | R (瓦特) |

#### 地址计算公式

```
第N路第M台FFU的寄存器地址:

状态地址 = 基址 + (路号-1)×60 + (FFU地址-1)
转速地址 = 0301 + (路号-1)×60 + (FFU地址-1)
功率地址 = 0601 + (路号-1)×60 + (FFU地址-1)
开关地址 = 1601 + (路号-1)×60 + (FFU地址-1)
设定地址 = 1231 + (路号-1)×60 + (FFU地址-1)
```

**举例**：第3路第15台FFU的实际转速

```
地址 = 0301 + (3-1)×60 + (15-1) = 0301 + 120 + 14 = 435
```

#### 全局批量控制（TCP协议独有）

| 寄存器 | 功能 | 说明 |
|--------|------|------|
| 1201 | 发送标志 | 只有值变化时才执行 |
| 1202 | 组选择 | 1~10=单组, 11=全部 |
| 1203 | 开关设定 | 0=全关, 1=全开 |
| 1204 | 转速设定 | 400~1500 RPM |

**批量控制用法**：先写1202(选组) → 1203(开关) → 1204(转速) → 1201(触发执行)

---

## 五、MCGS四大配置详解

### 5.1 配置关系图

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│  设备窗口     │────▶│  实时数据库   │◀────│  用户窗口     │
│  (通信配置)   │     │  (变量池)     │     │  (画面控件)   │
└──────────────┘     └──────────────┘     └──────────────┘
        │                   ▲                   │
        └───────────────────┴───────────────────┘
                    通道连接（变量↔寄存器）
```

### 5.2 设备窗口配置

**作用**：加载通信驱动，连接硬件设备

**配置步骤**：

1. 设备窗口 → 右键 → 插入设备
2. 选择：通用设备 → Modbus TCP
3. 配置属性：
   - IP地址：192.168.1.101
   - 端口号：502
   - 从机地址：0
   - 采集周期：1000ms

### 5.3 实时数据库配置

**作用**：定义所有变量（名字、类型、初始值）

**变量类型**：

| 类型 | 用途 | 示例 |
|------|------|------|
| 数值型 | 存数字 | FFU1_转速 = 1200 |
| 开关型 | 存开关状态 | FFU1_开关 = 1 |
| 字符型 | 存字符串 | 报警信息 = "Er01过流" |

**配置步骤**：

1. 实时数据库 → 新增
2. 填写变量名称、类型、读写属性、初始值

### 5.4 设备通道连接

**作用**：把变量和Modbus寄存器绑定

**配置步骤**：

1. 双击设备窗口中的Modbus TCP设备
2. 进入"通道连接"标签页
3. 点击"增加通道"
4. 设置功能码、寄存器地址、连接的数据对象

**示例**：

| 功能码 | 寄存器 | 数据对象 | 读写 |
|--------|--------|---------|------|
| 03 | 0001 | FFU1_状态 | 只读 |
| 03 | 0301 | FFU1_转速 | 只读 |
| 06 | 1601 | FFU1_开关 | 读写 |

### 5.5 用户窗口配置

**作用**：画交互界面

**关键设置**：

**设置启动窗口**：右键窗口 → 属性 → 勾选"在运行系统启动时自动打开此窗口"

**常用控件**：

| 控件 | 作用 |
|------|------|
| 按钮 | 点击执行操作 |
| 输入框 | 让用户输入数值 |
| 标签 | 显示文字/数值 |
| 位图 | 显示图片 |

### 5.6 运行策略配置

**作用**：脚本逻辑控制

**策略类型**：

| 类型 | 触发方式 | 用途 |
|------|---------|------|
| 启动策略 | 启动时执行一次 | 初始化变量 |
| 循环策略 | 周期循环执行 | 数据采集 |
| 事件策略 | 条件满足时执行 | 报警处理 |

---

## 六、按钮控制到底层通信

### 6.1 完整链路

```
点击按钮
    │
    ▼
用户窗口：按钮动画连接 → 写入变量
    │
    ▼
实时数据库：变量值改变
    │
    ▼
设备窗口：通道连接 → 变量绑定寄存器
    │
    ▼
设备驱动：Modbus驱动自动发报文
    │
    ▼
触屏控制器 → 中继器 → FFU响应
```

### 6.2 不需要写底层代码

MCGS的底层通信代码（Modbus发报文、CRC校验）已经封装在驱动里。

你只需要配置：
1. 按钮动画连接 → 绑定变量
2. 实时数据库 → 定义变量
3. 设备通道连接 → 变量绑定寄存器

### 6.3 配置按钮动画连接

1. 双击按钮 → 动画组态
2. 配置按键动画：

```
按下时:
  数据对象: FFU1_开关
  操作:     置1

松开时:
  数据对象: FFU1_开关
  操作:     置0
```

---

## 七、系统初始化与交互流程

### 7.1 启动顺序

```
系统上电
    │
    ▼
设备窗口初始化（加载驱动，连接设备）
    │
    ▼
启动策略执行一次（初始化变量）
    │
    ▼
主控窗口运行（后台调度）
    │
    ▼
主画面自动打开（用户看到的界面）
    │
    ▼
循环策略周期执行（数据采集）
```

### 7.2 主控窗口 vs 用户窗口

| 概念 | 作用 | 是否显示画面 |
|------|------|-------------|
| 主控窗口 | 后台"壳"，负责调度 | ❌ 不显示 |
| 用户窗口 | 真正的人机界面 | ✅ 显示 |

**第一个显示的是设置了"自动打开"的用户窗口。**

### 7.3 启动策略示例

```vb
!SetDevice(0, 6, "BACKLIGHTON(1800)")    ' 背光开1800秒
SV = 800                                   ' 设定值默认800
Alarm_OFF = 0                              ' 报警未屏蔽
SPEED_MAX = 1500                           ' 最大转速1500
```

---

## 八、常用脚本函数速查

### 8.1 设备操作函数

| 函数 | 说明 | 示例 |
|------|------|------|
| `!SetDevice(设备号, 6, "命令")` | 向设备发送命令 | `!SetDevice(0,6,"BACKLIGHTON()")` |

### 8.2 位操作函数

| 函数 | 说明 | 示例 |
|------|------|------|
| `!BitSet(变量, 位号)` | 置位 | `!BitSet(Alarm_Buzz_En, 0)` |
| `!BitClear(变量, 位号)` | 清位 | `!BitClear(Alarm_Buzz_En, 1)` |
| `!BitLShift(值, 位数)` | 左移 | `!BitLShift(sensor0007, 16)` |

### 8.3 界面操作函数

| 函数 | 说明 |
|------|------|
| `!SetDialogBy9Palace()` | 触摸屏九点校准 |
| `!OpenWindow(窗口名)` | 打开指定窗口 |

---

## 九、常见问题排查

### 9.1 网络问题

| 现象 | 原因 | 解决方法 |
|------|------|---------|
| 以太网显示"断开" | 网线松动 | 重新插紧网线 |
| ping不通 | 不在同一网段 | 设置相同网段IP |
| ping超时 | 防火墙拦截 | 关闭防火墙测试 |

### 9.2 通信问题

| 现象 | 原因 | 解决方法 |
|------|------|---------|
| 数据全为0 | 通信未建立 | 检查设备窗口配置 |
| 数据异常 | 字节序不对 | 确认Big-Endian |
| 写入无效 | Slave ID错误 | TCP协议必须为0 |

### 9.3 U盘升级问题

| 现象 | 原因 | 解决方法 |
|------|------|---------|
| 路径不合法 | 选了子文件夹 | 必须选U盘根目录 |
| 不是合法U盘 | 目标是硬盘 | 必须用真实U盘 |

---

## 附录：快速配置清单

### 新增一台FFU控制

- [ ] 实时数据库：新增变量（状态、转速、功率、开关、设定）
- [ ] 设备窗口：新增通道连接（绑定变量到寄存器）
- [ ] 用户窗口：新增按钮和显示标签
- [ ] 动画连接：绑定控件到变量

### 系统启动检查

- [ ] 网线连接正常（以太网显示"已连接"）
- [ ] IP设置正确（同一网段）
- [ ] 触屏IP确认（默认192.168.1.101）
- [ ] 程序已下载到触屏
- [ ] 主画面设置了"自动打开"

---

## 参考资源

1. 昆仑通态官网 — 下载McgsPro软件和帮助文档
2. MCGS自带帮助 — 软件内按F1
3. B站搜索"MCGS组态教程" — 视频教程

---

*文档生成日期：2026-07-01*
*基于项目：V9.22_CAD版本_触屏15寸_三桥logo_3组*
