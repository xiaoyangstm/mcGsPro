# MCGS Pro 学习笔记 - 2026年6月29日

## 一、识别 .mdr 文件类型

### 问题
用户有一个 `.mdr` 文件（串口2透传.mdr），不知道是什么文件。

### 解决方法
- **文件类型**：MCGS（昆仑通态）设备驱动文件
- **文件结构**：前6字节为文件头，之后是 zlib 压缩的数据
- **编码**：GBK（中文）
- **用途**：用于 MCGS 组态软件的设备驱动，实现串口 ModBus RTU 透传通信

### 文件包含的功能模块
1. **通道定义** — 定义了 AI01~AI20 共20个浮点通道
2. **设备命令脚本**：
   - `WriteStr` — 串口写字符串命令
   - `ReadStr` — 串口读字符串命令（带超时参数）
   - `WriteModBus` — ModBus 写命令（功能码 06）
   - `ReadModBus` — ModBus 读命令（功能码 03）
3. **生命周期脚本**：CollectData、SingleChanWrite、InitDevice、ExitDevice

---

## 二、MCGS 基础函数理解

### 1. !SetDevice 函数

**函数格式**：
```
!SetDevice(设备名称, 操作码, "命令字符串")
```

| 参数 | 说明 |
|-----|------|
| `设备名称` | 目标设备的名称 |
| `操作码` | 操作类型代码，`6` 表示调用设备方法/命令 |
| `命令字符串` | 要执行的命令 |

**示例**：
```vbscript
' 修改串口端口号
!SetDevice(通用串口父设备0, 6, "ChangePort(1)")       ' 切换到COM1
!SetDevice(通用串口父设备0, 6, "ChangePort(Data00)")  ' 用变量控制端口号
```

### 2. SetLocal 函数（TCPIP设备）

**用途**：设置本地IP地址和端口号

**示例**：
```vbscript
!SetDevice(通用TCPIP父设备0, 6, "SetLocal(200.200.200.190,0)")  ' 固定IP，随机端口
!SetDevice(通用TCPIP父设备0, 6, "SetLocal(LocalIP,Port)")       ' 用变量控制
```

### 3. SetWindow 函数

**用途**：运行时打开/关闭用户窗口（不是控制按钮显示）

```vbscript
!SetWindow(窗口名称, 1)   ' 1 = 打开窗口
!SetWindow(窗口名称, 0)   ' 0 = 关闭窗口
```

---

## 三、界面显示问题排查

### 问题
用户界面显示空白或出厂界面，不是自己画的界面。

### 原因分析
| 问题 | 原因 | 解决方法 |
|-----|------|---------|
| 显示空白 | 启动窗口未设置 | 设置启动窗口 |
| 显示出厂界面 | 工程未下载成功 | 重新下载 |
| 一会儿有、一会儿空白 | 封面窗口设成了主画面 | 封面窗口改为"无" |

### 关键概念区分

| 窗口类型 | 作用 | 类比 |
|---------|------|------|
| **封面窗口** | 开机时显示的过渡画面，**几秒后自动消失** | 手机开机动画 |
| **启动窗口** | 开机后真正显示的主界面，**一直存在** | 手机桌面 |
| **内存窗口** | 常驻内存，切换快 | 后台常驻程序 |

### 正确配置
```
封面窗口: 无        ← 关键！不要放主画面
封面显示时间: 0秒
启动窗口: 主画面
用户窗口列表: 空（留空即可）
```

### 必须先保存编译再运行
**模拟运行也需要先保存+编译/下载！**
```
Ctrl+S 保存 → F7 编译 → F5 模拟运行
```

---

## 四、数据对象与控件关联

### 问题
报错"请输入---未知对象!"

### 原因
在界面上使用了变量，但数据库里没有创建该数据对象。

### 解决方法
1. 打开 **「实时数据库」**
2. 新建数据对象（设置名称、类型、初始值）
3. 在控件属性中关联该数据对象

### 数据对象类型

| 类型 | 用途 |
|------|------|
| **开关型数据** | 布尔值（真/假） |
| **数值型数据** | 整数 |
| **浮点型数据** | 小数（如温度、压力） |
| **字符串型数据** | 文本 |

---

## 五、输入框控件使用

### 问题
不知道如何让输入框在屏幕上输入数字。

### 解决方法
**不需要写脚本！直接关联数据对象即可。**

1. 双击输入框 → 属性设置
2. 在 **「操作属性」** 或 **「基本属性」** 中关联数据对象
3. 运行时点击输入框 → 弹出软键盘 → 输入 → 确认
4. 值自动写入关联的数据对象

### 输入框事件（可选脚本）

| 事件 | 触发时机 |
|------|---------|
| `AfterWrite` | 输入完成后触发 |
| `BeforeWrite` | 输入完成、写入前触发 |
| `Change` | 内容改变时触发 |

---

## 六、Modbus RTU 配置 FFU 风机控制器

### 通讯参数（协议文档）

| 参数 | 值 |
|-----|-----|
| 协议 | Modbus RTU |
| 从站地址 | 1（默认） |
| 波特率 | **19200** bps |
| 数据位 | 8 |
| 停止位 | 1 |
| 校验位 | 无 |
| 数据格式 | 低字节在前(L-H) |
| 支持功能码 | 03(读)、06(写单)、10(写多) |

### 配置步骤

#### 第一步：创建实时数据库

端口1第1台FFU的数据对象：

| 对象名称 | 类型 | 用途 |
|---------|------|------|
| `端口1_台1_状态` | 数值型数据 | 0=停止，1=运行，≥2=故障 |
| `端口1_台1_实际转速` | 数值型数据 | 实际转速 rpm |
| `端口1_台1_实际功率` | 数值型数据 | 实际功率 W |
| `端口1_台1_转速设定` | 数值型数据 | 设定转速 400~1500 rpm |
| `端口1_端口台数` | 数值型数据 | 端口连接台数 |
| `端口1_台1_开关` | 数值型数据 | 0=关闭，1=启动 |
| `端口1_台1_运行时间` | 数值型数据 | 运行时间 小时 |

#### 第二步：配置设备窗口

**串口父设备参数**：
| 参数 | 设置值 |
|-----|-------|
| 波特率 | **19200** ← 关键 |
| 数据位 | 8 |
| 停止位 | 1 |
| 校验位 | 无 |

**Modbus 设备属性**：
| 参数 | 值 |
|-----|-----|
| 从站地址 | 1 |
| 通讯超时 | 1000 ms |
| 采集周期 | 1000 ms |

#### 第三步：添加设备通道

⚠️ **重要**：MCGS 里的通道类型名称与标准 Modbus 不同

| MCGS 通道类型 | Modbus 标准名称 | 寄存器区 |
|-------------|---------------|---------|
| 输入继电器 | 输入线圈 | 1区 |
| 输出继电器 | 输出线圈 | 0区 |
| 输入寄存器 | 输入寄存器 | 3区 |
| **输出寄存器** | **保持寄存器** | **4区** ← FFU用这个 |

**通道配置表（端口1第1台）**：

全部选 **「输出寄存器」**：

| 序号 | 通道类型 | 寄存器地址 | 数据类型 | 读写属性 | 关联变量 |
|-----|---------|-----------|---------|---------|---------|
| 1 | 输出寄存器 | 1 | 16位无符号 | 只读 | `端口1_台1_状态` |
| 2 | 输出寄存器 | 301 | 16位无符号 | 只读 | `端口1_台1_实际转速` |
| 3 | 输出寄存器 | 601 | 16位无符号 | 只读 | `端口1_台1_实际功率` |
| 4 | 输出寄存器 | 1226 | 16位无符号 | 读写 | `端口1_端口台数` |
| 5 | 输出寄存器 | 1231 | 16位无符号 | 读写 | `端口1_台1_转速设定` |
| 6 | 输出寄存器 | 1601 | 16位无符号 | 读写 | `端口1_台1_开关` |
| 7 | 输出寄存器 | 4001 | 16位无符号 | 只读 | `端口1_台1_运行时间` |

**地址填写说明**：直接填协议里的相对地址（不加 40000 前缀），因为选了"输出寄存器"驱动会自动处理。

#### 第四步：用户窗口画界面

**控件布局**：
```
┌──────────────────────────────────────────────────┐
│            FFU风机控制器 - 端口1                  │
│  [  启动  ]    [  停止  ]      ● 运行指示灯       │
│  转速设定: [________] rpm  (400~1500)            │
│  ─────────── 运行状态 ───────────                 │
│  运行状态:  [________]                            │
│  实际转速:  [________] rpm                        │
│  实际功率:  [________] W                          │
│  运行时间:  [________] 小时                       │
└──────────────────────────────────────────────────┘
```

**按钮脚本**：
```vbscript
' 启动按钮
端口1_台1_开关 = 1

' 停止按钮
端口1_台1_开关 = 0
```

#### 第五步：主控窗口启动脚本

**初始化端口台数**：
```vbscript
端口1_端口台数 = 1
```

位置：主控窗口 → 属性 → 启动脚本

#### 第六步：保存编译下载

```
Ctrl+S 保存 → F7 编译 → 下载 → 断电重启触摸屏
```

---

## 七、FFU 寄存器地址规律

### 端口1多台FFU地址规律

| FFU编号 | 状态地址 | 转速(实际) | 转速(设定) | 开关地址 |
|--------|---------|-----------|-----------|---------|
| 第1台 | 1 | 301 | 1231 | 1601 |
| 第2台 | 2 | 302 | 1232 | 1602 |
| 第N台 | N | 300+N | 1230+N | 1600+N |

---

## 八、密码验证脚本解析

```vbscript
if 标志=1 then
    if !strComp(密码1,密码对比)=0 then
        ' 密码正确
        密码权限=1
        用户窗口.密码管理.Open( )
        用户窗口.密码窗.Close( )
    else
        ' 密码错误，根据语言显示不同提示
        n = !GetCurrentLanguageIndex( )
        if(n = 0) THEN
            message="密码错误!请重新输入！"
        ELSE
            message="Password Error!Please Check！"
        ENDIF
        密码权限=0
    endif
    标志=0
    
    ' 后门密码：88888888
    if !strComp(密码1,"88888888")=0 then
        用户窗口.密码管理.Open( )
        用户窗口.密码窗.Close( )
    endif
    exit
endif
```

**脚本功能说明**：
| 代码 | 功能 |
|-----|------|
| `!strComp(密码1,密码对比)=0` | 比较两个字符串是否相等，返回0表示相等 |
| `!GetCurrentLanguageIndex()` | 获取当前语言索引，0为中文 |
| `用户窗口.密码管理.Open()` | 打开密码管理窗口 |
| `用户窗口.密码窗.Close()` | 关闭密码输入窗口 |
| `"88888888"` | 后门密码，直接打开管理窗口 |

---

## 九、常见问题汇总

| 问题 | 原因 | 解决方法 |
|-----|------|---------|
| 报错"未知对象" | 数据对象未创建 | 在数据库创建对象 |
| 界面空白/灰屏 | 封面窗口设成主画面 | 封面改为"无" |
| 界面显示出厂内容 | 工程未下载成功 | 清除原工程后重新下载 |
| 模拟运行看不到界面 | 没保存编译就运行 | Ctrl+S → F7 → F5 |
| 控件不显示 | 画错窗口或超出画布 | 检查控件位置 |
| 输入框无法输入 | 未关联数据对象 | 关联数据对象 |
| Modbus 通讯失败 | 波特率错误 | 确认19200 |
| 通道类型找不到保持寄存器 | MCGS命名不同 | 选"输出寄存器" |
| 转速设不进去 | 端口台数未初始化 | 启动脚本设台数 |

---

## 十、完整操作流程总结

```
第一步：创建数据对象（实时数据库）
    ↓
第二步：配置设备窗口（串口+Modbus）
    ↓
第三步：添加设备通道
    ↓
第四步：用户窗口画界面
        - 添加标签（文字）
        - 添加输入框（关联变量）
        - 添加按钮（写脚本）
        - 添加指示灯
    ↓
第五步：主控窗口启动脚本（初始化）
    ↓
第六步：保存 → 编译 → 下载 → 重启
    ↓
测试通讯和控制
```

---

## 参考资源

- MCGS Pro 版本：3.3.6.6354
- FFU Modbus RTU 5路通讯协议定义V5.01
- GitHub仓库：https://github.com/xiaoyangstm/mcGsPro.git

---

**整理日期**：2026年6月29日
**整理人**：Claude Code AI助手