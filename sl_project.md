# MCGS系统初始化与交互流程

---

## 一、启动顺序

```
系统上电 / 程序下载运行
        │
        ▼
┌─────────────────────────────┐
│  1. 设备窗口                 │  ← 先初始化！加载驱动、连接设备
│     (所有设备窗口的策略)      │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│  2. 运行策略 ─ 启动策略       │  ← 执行一次，做初始化变量赋值
│     (脚本只执行一次)          │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│  3. 主控窗口（MCGS运行环境）  │  ← 这里是"壳"，不是画面
│     (后台运行，不显示画面)    │
└─────────────┬───────────────┘
              │
              ▼
┌─────────────────────────────┐
│  4. 用户窗口 ─ 主画面         │  ← 第一个用户窗口打开，显示给用户
│     (真正的人机交互界面)      │
└─────────────────────────────┘
```

> 💡 **关键点**：主控窗口不显示画面，它只是个"壳"负责运行环境调度。第一个显示的是**用户窗口**里设置了"自动打开"的主画面。

---

## 二、四大配置的作用与关系

```
┌──────────────────────────────────────────────────────────────┐
│                        你的触摸屏                             │
│                                                              │
│  ┌──────────────┐     ┌──────────────┐     ┌──────────────┐  │
│  │  设备窗口     │────▶│  实时数据库   │◀────│  用户窗口     │  │
│  │  (通信配置)   │     │  (变量池)     │     │  (画面控件)   │  │
│  └──────────────┘     └──────────────┘     └──────────────┘  │
│         │                   ▲                   │            │
│         │                   │                   │            │
│         └───────────────────┴───────────────────┘            │
│                    通道连接（变量↔寄存器）                     │
└──────────────────────────────────────────────────────────────┘
```

| 配置项 | 作用 | 类比 |
|--------|------|------|
| **设备窗口** | 管理通信驱动、连接硬件 | 决定"和谁说话" |
| **实时数据库** | 定义所有变量（名字、类型、初始值） | 决定"说什么话的内容" |
| **用户窗口** | 画界面（按钮、显示、动画） | 决定"怎么说（交互方式）" |
| **运行策略** | 脚本逻辑，控制何时干什么 | 决定"什么时候说" |

---

## 三、设备窗口配置（详细步骤）

### 步骤1：打开设备窗口

```
McgsPro左侧工作区 → 设备窗口 → 双击打开
```

### 步骤2：添加父设备（通信驱动）

1. 在设备窗口空白处 **右键 → 插入设备**
2. 选择设备分类，比如 **通用设备 → Modbus TCP**
3. 点击确定，设备添加到窗口

### 步骤3：配置父设备属性

双击父设备（Modbus TCP），配置：

```
┌─ Modbus TCP 属性 ─────────────────────────┐
│  设备名称:    ModbusTCP_1                  │
│  设备注释:    FFU通信                       │
│  通信方式:    以太网                         │
│  IP地址:      192.168.1.101    ← 触屏的IP  │
│  端口号:      502               ← Modbus端口│
│  通信超时:    2000ms                       │
│  重试次数:    3                            │
│  采集周期:    1000ms          ← 多久读一次  │
│  最大从机数:  1                            │
│  从机地址:    0                ← 固定0      │
└───────────────────────────────────────────┘
```

### 步骤4：建立通道连接（关键！）

把**变量**和**设备寄存器**绑定：

1. 双击父设备 → 进入"通道连接"标签页
2. 点击"增加通道"
3. 选择功能码（如03读保持寄存器）
4. 输入寄存器地址（如1601）
5. 选择**连接的数据对象**（实时数据库里已有的变量名）
6. 设置读写属性（只读/读写）

```
┌─ 通道连接 ──────────────────────────────────────────────────┐
│  功能码 │ 寄存器地址 │ 数据对象名称      │ 读写  │ 通道注释   │
│─────────┼───────────┼──────────────────┼──────┼───────────│
│   03    │   0001    │ FFU1_状态         │ 只读  │ 第1路1号状态│
│   03    │   0301    │ FFU1_转速         │ 只读  │ 第1路1号转速│
│   03    │   0601    │ FFU1_功率         │ 只读  │ 第1路1号功率│
│   06    │   1601    │ FFU1_开关         │ 读写  │ 第1路1号开关│
│   06    │   1231    │ FFU1_转速设定     │ 读写  │ 第1路1号设定│
└─────────────────────────────────────────────────────────────┘
```

---

## 四、实时数据库配置（详细步骤）

### 步骤1：打开实时数据库

```
McgsPro左侧工作区 → 实时数据库 → 双击打开
```

### 步骤2：新增变量

```
┌─ 变量属性 ──────────────────────────────────────┐
│  变量名称:    FFU1_状态                          │
│  变量注释:    第1路1号FFU运行状态                 │
│  变量类型:    数值型                              │
│  读写属性:    只读                                │
│  初始值:      0                                  │
└──────────────────────────────────────────────────┘
```

### 步骤3：变量类型说明

| 类型 | 用途 | 示例 |
|------|------|------|
| **数值型** | 存数字（转速、功率、温度等） | FFU1_转速 = 1200 |
| **开关型** | 存开关状态（0或1） | FFU1_开关 = 1 |
| **字符型** | 存字符串 | 报警信息 = "Er01过流" |

---

## 五、用户窗口配置（详细步骤）

### 步骤1：新建窗口

```
用户窗口 → 右键 → 新建窗口 → 命名（如"主画面"）
```

### 步骤2：设置启动窗口（重要！）

```
右键"主画面" → 属性 → 窗口属性
    ☑ 在运行系统启动时自动打开此窗口   ← 勾上！
```

### 步骤3：给按钮配置动画连接

1. 从工具箱拖一个**按钮**到画面上
2. 双击按钮 → 弹出"动画组态"对话框
3. 配置**按键动画**：

```
┌─ 按键动画 ──────────────────────────────────┐
│  按下时:                                      │
│    数据对象: FFU1_开关                         │
│    操作:     置1                               │
│                                              │
│  松开时:                                      │
│    数据对象: FFU1_开关                         │
│    操作:     置0                               │
└──────────────────────────────────────────────┘
```

### 步骤4：给数值显示配置动画连接

```
┌─ 输出动画 ──────────────────────────────────┐
│  数据对象:    FFU1_转速                        │
│  格式:        整数                             │
│  小数位数:    0                               │
│  输出值:      [FFU1_转速] RPM                 │
└──────────────────────────────────────────────┘
```

---

## 六、运行策略配置

### 三种策略类型

| 策略类型 | 触发方式 | 用途 |
|---------|---------|------|
| **启动策略** | 程序启动时执行一次 | 初始化变量、设默认参数 |
| **循环策略** | 按设定周期循环执行 | 定期采集数据、计算逻辑 |
| **事件策略** | 条件满足时执行一次 | 报警触发、特殊事件处理 |

### 启动策略示例

```vb
!SetDevice(0, 6, "BACKLIGHTON(1800)")    ' 背光开1800秒
SV = 800                                   ' 设定值默认800
Alarm_OFF = 0                              ' 报警未屏蔽
SPEED_MAX = 1500                           ' 最大转速1500
DISP_MODE = 0                              ' 显示模式0
```

---

## 七、完整配置示例：控制一台FFU

### 第1步：实时数据库建变量

| 变量名 | 类型 | 读写 | 注释 |
|--------|------|------|------|
| FFU1_状态 | 数值型 | 只读 | 设备状态 |
| FFU1_转速 | 数值型 | 只读 | 实际转速 |
| FFU1_功率 | 数值型 | 只读 | 实际功率 |
| FFU1_开关 | 开关型 | 读写 | 启停控制 |
| FFU1_转速设定 | 数值型 | 读写 | 转速设定 |

### 第2步：设备窗口建通道连接

| 功能码 | 寄存器 | 数据对象 | 读写 |
|--------|--------|---------|------|
| 03 | 0001 | FFU1_状态 | 只读 |
| 03 | 0301 | FFU1_转速 | 只读 |
| 03 | 0601 | FFU1_功率 | 只读 |
| 06 | 1601 | FFU1_开关 | 读写 |
| 06 | 1231 | FFU1_转速设定 | 读写 |

### 第3步：用户窗口画画面

- 拖一个**按钮**，动画连接：按下→置1到FFU1_开关（启动）
- 拖一个**按钮**，动画连接：按下→置0到FFU1_开关（停止）
- 拖三个**标签**，分别显示FFU1_状态、FFU1_转速、FFU1_功率

### 第4步：运行策略初始化

```vb
FFU1_开关 = 0
FFU1_转速设定 = 800
```

---

## 八、启动流程图（完整版）

```
触屏上电 / 程序启动
        │
        ▼
┌─────────────────┐
│ 设备窗口初始化    │ ← 加载Modbus TCP驱动，连接触屏IP 192.168.1.101
│ 建立通信通道      │
└───────┬─────────┘
        │
        ▼
┌─────────────────┐
│ 启动策略执行一次  │ ← 初始化变量、背光、默认参数
│ (脚本)           │
└───────┬─────────┘
        │
        ▼
┌─────────────────┐
│ 主控窗口运行      │ ← 调度用户窗口、策略、设备通信
│ (后台"壳")       │
└───────┬─────────┘
        │
        ▼
┌─────────────────┐
│ 主画面自动打开    │ ← 用户看到的第一个画面
│ (设置了自动打开)  │
└───────┬─────────┘
        │
        ▼
┌─────────────────┐
│ 循环策略周期执行  │ ← 1秒1次：采集FFU数据、更新显示
│ (每秒)           │
└─────────────────┘
```

---

## 九、配置检查清单

- [ ] **设备窗口**：确认Modbus TCP驱动已添加，IP=192.168.1.101，端口502，SlaveID=0
- [ ] **通道连接**：确认变量和寄存器已绑定（通道连接表）
- [ ] **实时数据库**：确认FFU相关变量已建好
- [ ] **用户窗口**：确认主画面设置了"启动时自动打开"
- [ ] **运行策略**：确认启动策略有初始化脚本，循环策略有数据采集逻辑
- [ ] **按钮动画**：确认启停按钮的动画连接绑定了正确的变量
